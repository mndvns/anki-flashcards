In the following example, `favorite` is an object within the `person` object. We call this {{c1::a nested object}}.

```js
const person = {
  name: "Ryan",
  state: "UT",
  favorite: {
    food: "pizza",
    drink: "water",
    day: "Saturday"
  }
};
```
